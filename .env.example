# BitRymDym — environment placeholders
# Copy to .env.local and fill with real values locally.
# NEVER commit secrets. NEVER put service role keys in NEXT_PUBLIC_*.

# Supabase (OD-03) — public client values only for NEXT_PUBLIC_*
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — do not expose to the browser
SUPABASE_SERVICE_ROLE_KEY=
